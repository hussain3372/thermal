import ClientLayout from "@/app/ClientLayout";
import React from "react";
import Cancel from "./Cancel";

const page = () => {
  return (
    <div>
      <ClientLayout>
        <Cancel />
      </ClientLayout>
    </div>
  );
};

export default page;
